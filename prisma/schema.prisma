generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
}

model Subject {
  id          Int      @id @default(0)
  nombre      String   @default("Sin nombre")
  descripcion String   @default("Sin descripción") 
}
